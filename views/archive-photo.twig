{% extends "base.twig" %}

{% block content %}
    <div class="teases">
        {% for post in posts %}
            {% include ['tease-photo.twig', 'tease.twig'] %}
        {% endfor %}
    </div>

    {% include 'partial/pagination.twig' with { pagination: posts.pagination({show_all: false, mid_size: 3, end_size: 2}) } %}
{% endblock %}
